---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---
<BaseLayout pageTitle={frontmatter.title} >
  <div class="font-lora py-8 md:py-20 max-w-3xl mx-auto text-lg md:text-xl md:leading-8 blog-post">
    <slot />
  </div>
</BaseLayout>