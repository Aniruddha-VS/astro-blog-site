---
import BlogPostHeading from "./BlogPostHeading.astro";
const pageHeading = "Places"

const allPosts = await Astro.glob('../pages/posts/*.mdx')

---
<div class="flex flex-col font-lora py-20 max-w-3xl mx-auto text-lg md:text-xl md:leading-8">
    <BlogPostHeading title={pageHeading}/>
    <div class="pt-8">
    <p>Here are some of the places I have been to...<p/>
        <br/>
    {allPosts.map((place) => <li class="hover:text-teal-700"> <a href={place.url}>{place.frontmatter.location}</a></li>)}
    <div/>
<div>