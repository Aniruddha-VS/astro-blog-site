---
const {imageUrl, imageAltText} = Astro.props

const images = import.meta.glob('/src/assets/*.{jpeg,jpg,png,gif}')

---


<img  class="py-8 h-[32rem] object-cover w-full" src={images[imageUrl]().then((resolve) => resolve)} alt={imageAltText } />
